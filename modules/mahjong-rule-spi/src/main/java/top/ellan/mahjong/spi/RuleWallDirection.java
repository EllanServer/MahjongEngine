package top.ellan.mahjong.spi;

/** Direction in which logical wall stacks advance from the declared break. */
public enum RuleWallDirection {
    /** Advance through wall stacks clockwise from the declared break. */
    CLOCKWISE,
    /** Advance through wall stacks counterclockwise from the declared break. */
    COUNTERCLOCKWISE
}
